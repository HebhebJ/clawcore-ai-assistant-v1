# Web Research Skill

- Prefer `search_web` to discover candidate sources first.
- Use `read_url` only on selected high-signal URLs.
- Summarize findings with source links and uncertainty notes.
- Avoid low-signal pages and repeated duplicate reads.
